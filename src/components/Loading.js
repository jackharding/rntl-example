import React from 'react';
import {
    Text,
    StyleSheet,
    View,
} from 'react-native';


const Loading = (props) => (
    <View>
        <Text>Loading</Text>
    </View>
);

Loading.defaultProps = {
    style: {}
}

export default Loading;